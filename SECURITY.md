# 安全声明

星语 AI 重视用户数据安全与隐私。本应用的设计原则是**数据本地优先**：所有角色、对话、记忆与设置均保存在设备本地，不收集、不上传任何个人信息。

## 安全特性

- **数据本地存储**：全部数据仅保存在设备 localStorage，不上传任何服务器
- **内置 AI 完全离线**：内置模型推理在本机进行，不产生任何网络请求
- **API Key 本地保存**：用户填写的接口 Key 只保存在本机，仅在请求时随请求发出
- **PWA / WebView 无遥测**：应用不包含任何统计、埋点或遥测代码

## 报告安全漏洞

如果发现安全漏洞，请**不要**公开提交 Issue（避免被恶意利用）。请直接通过以下方式私下报告：

- 在本仓库创建安全通告：点击仓库页面的 **Security → Report a vulnerability**
- 或发送邮件联系维护者（在仓库页面查看）

请在报告中包含：

1. 漏洞类型与影响范围
2. 复现步骤（或 PoC）
3. 受影响的版本
4. 修复建议（如有）

维护者会在收到报告后尽快响应与处理。

## 安全最佳实践（给用户）

- 使用 OpenAI 兼容接口时，请勿将 Key 分享给他人
- 不要在对话中透露真实的银行卡、密码等敏感信息（AI 回复不构成任何专业建议）
- 从官方渠道下载 APK，避免安装来路不明的安装包
